namespace Photon.App;

/// <summary>Placeholder — replaced by the real main window during the module build.</summary>
public partial class MainForm : Form
{
    public MainForm()
    {
        Text = "Photon";
        ClientSize = new Size(1400, 850);
    }
}
